### 分组树状数组
有很多个组，每个组内部都要做“单点取最大值 + 前缀最大值查询”，但每个组的坐标数量不同，希望总空间只有 O(n)。
最自然的写法是
vector<BIT> tree(n + 1);
然后每个 tree[x] 自己保存一个 vector<int>
但对于 $n \le 10^6$ 的规模，大量小数组会带来大量动态内存分配并且每个vector都需要存储自身的信息
分组树状数组通过让所有组的 BIT 共用一个大数组来优化该问题
[2026ICPC网络赛1 N](https://qoj.ac/contest/4071/problem/20029)
普通线段树为存储 O(n) 的数组需要开 O(4 * n) 的节点来存储信息，对于 n 很大的情况无法接受。
如果操作数 m 较小，根据线段树的复杂度分析，每次修改只会影响 O(logn) 个节点，也就是说，被修改操作影响的节点只有 O(mlogn) 的量级，只需要在节点被用到时动态开点，其余未用到的的点不用建出来。

模板
[luoguP13825](https://www.luogu.com.cn/problem/P13825)
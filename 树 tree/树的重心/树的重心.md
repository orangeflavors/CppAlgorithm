### 树的重心
树的重心有如下三种定义,求出来的点是一样的
1. 以某个节点为根时,最大子树的节点数最少,那么这个节点是重心
2. 以某个节点为根时,每颗子树的节点数不超过总节点数的一半,那么这个节点是重心
3. 以某个节点为根时,所有节点都走向该节点的总边数最少,那么这个节点是重心

补充性质:
4. 一棵树最多有两个重心,如果有两个重心,那么两个重心一定相邻
5. 如果树上增加或者删除一个叶节点,转移后的重心最多移动一条边
6. 如果把两棵树连起来,那么新树的重心一定在原来两棵树重心的路径上
7. 树上的边权如果都>=0,不管边权怎么分布,所有节点都走向重心的总距离和最小

注意“重心最多只有两个”这个性质需要正点权。

#### 求最小带权路径和
即计算
$$
\sum_{u = 1}^n w_u \times dist(c, u)
$$
任选一个重心计算带权路径和
- 如果 u-v 之间的边权 > 0，
  则 u、v 两侧的点权必须恰好各占总点权的一半，
  所以把重心从 u 移到 v 时，
  距离和不会变化。

- 如果 u-v 之间的边权 = 0，
  那么移动重心时所有相关距离本身就不会变化。

因此所有重心得到的 pathweight 相同。
特别地，当存在 0 点权时，centroids 可能很多，但仍然只需要选一个。

[luoguP1364](https://www.luogu.com.cn/problem/P1364)
[luoguP2986](https://www.luogu.com.cn/problem/P2986) 带非负整数边权带非负整数点权树的重心求最小带权路径和

#### 增删边使树的重心唯一
[codeforces1406 C](https://codeforces.com/contest/1406/problem/C)